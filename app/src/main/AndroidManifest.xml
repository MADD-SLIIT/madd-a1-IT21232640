<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication1">

        <activity
            android:name=".ReviewConfirmation"
            android:exported="false" />

        <activity
            android:name=".AllFeedback"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".MainActivity"
            android:exported="true">  <!-- Correct: Main entry point of the app -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Home"
            android:exported="true" />  <!-- Ensure this is correct based on your requirements -->

        <activity
            android:name=".registrationPage"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".loginPage"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".Profile"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".HotelView"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".hotelBooking"
            android:exported="false" />  <!-- Good: Internal use only -->

        <activity
            android:name=".ReviewPage"
            android:exported="false" />  <!-- Should be false, as it is used for submitting/reviewing -->

    </application>
</manifest>
